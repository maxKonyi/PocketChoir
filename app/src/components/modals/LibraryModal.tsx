/* ============================================================
   LIBRARY MODAL — RE-EXPORT FROM NEW PRESET BROWSER

   The old LibraryModal has been replaced by the full-featured
   PresetBrowser component.  This file re-exports it under the
   same name so existing imports continue to work.
   ============================================================ */

export { LibraryModal, default } from './PresetBrowser';
